[project]
name = "ritgard"
version = "0.1.0"
description = "The data-processing bits of Ritgard."
requires-python = ">=3.13"
dependencies = [
    "bertopic>=0.17.3",
    "matplotlib>=3.10.6",
    "sentence-transformers>=5.1.1",
    "umap-learn>=0.5.9.post2",
]

[dependency-groups]
dev = [
    "benchmark-imports>=1.0.0",
]
